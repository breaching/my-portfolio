# ==========================================
# Configuration Backend - Portfolio API
# ==========================================
# Copier ce fichier en .env et remplir les valeurs
#
# ATTENTION: En production, certaines valeurs sont OBLIGATOIRES
# L'application refusera de demarrer si elles ne sont pas correctes

# ==========================================
# ENVIRONNEMENT
# ==========================================
# development | staging | production
# En production: validation stricte des parametres de securite
ENVIRONMENT=development

# Mode debug (DOIT etre false en production)
DEBUG=false

# ==========================================
# BASE DE DONNEES
# ==========================================
DATABASE_URL=sqlite+aiosqlite:///./portfolio.db

# ==========================================
# SECURITE - CRITIQUE
# ==========================================
# Clé API admin (OBLIGATOIRE en production, minimum 32 caracteres)
# Generer avec: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_API_KEY=

# CORS - Origines autorisées (JSON array)
# En production: NE PAS utiliser localhost
CORS_ORIGINS=["http://localhost:3000"]

# Hosts autorisés (JSON array)
# En production: NE PAS utiliser "*"
ALLOWED_HOSTS=["localhost", "127.0.0.1"]

# HSTS (DOIT etre true en production avec HTTPS)
ENABLE_HSTS=false

# Taille max des requetes (bytes) - protection contre les attaques
MAX_REQUEST_SIZE=5000000

# ==========================================
# RATE LIMITING
# ==========================================
ENABLE_RATE_LIMITING=true
RATE_LIMIT_CALLS=100
RATE_LIMIT_PERIOD=60

# ==========================================
# LOGGING
# ==========================================
# Niveau: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# Format: json (recommande pour production) | text (dev)
LOG_FORMAT=json

# ==========================================
# CSP REPORTING (optionnel)
# ==========================================
# URI pour recevoir les rapports de violation CSP
# Laisser vide pour desactiver
CSP_REPORT_URI=/csp-report

# ==========================================
# SMTP - Notifications email (optionnel)
# ==========================================
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
CONTACT_EMAIL=

# ==========================================
# EXEMPLE CONFIGURATION PRODUCTION
# ==========================================
# ENVIRONMENT=production
# DEBUG=false
# ADMIN_API_KEY=votre_cle_secrete_de_minimum_32_caracteres
# CORS_ORIGINS=["https://votre-domaine.com"]
# ALLOWED_HOSTS=["votre-domaine.com", "www.votre-domaine.com"]
# ENABLE_HSTS=true
# LOG_LEVEL=WARNING
# LOG_FORMAT=json
